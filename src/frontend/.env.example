# Frontend dev defaults — copy to `.env.local` and override per machine.
# In dev, the Vite proxy at vite.config.js forwards /api/* to http://localhost:3001.
# In production, set this to /api (Vercel routes /api/* to the serverless function).
VITE_API_URL=/api
