# Frontend dev defaults — copy to `.env.local` and override per machine.
# The API base URL. Override with `VITE_API_URL=https://your-api.example.com`
# in the Vercel project settings when deploying.
VITE_API_URL=http://localhost:3001
